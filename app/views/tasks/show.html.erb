<button id="back" type="button" class="btn btn-link">Back</button>

<br />
<br />
<h3><%= current_url %></h3>

<div class="card">
  <div class="card-body">
    <h3><%= @task.title %></h3>
    <p><%= @task.description %></p>
    <%= due_date(@task.due_at) %>
    <span class="float-right"><%= link_to 'Edit', edit_task_path(@task) %></span>
  </div>
</div>
